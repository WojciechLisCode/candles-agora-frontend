// import {} from "./actions";

const initialState = {
  message: "empty",
};

export default function reducer(state = initialState, action) {
  switch (action.type) {
    default:
      return state;
  }
}
